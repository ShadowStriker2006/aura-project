{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "aura-runtime",
  "description": "Grants event and command permissions to both the main dashboard and overlay windows",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
